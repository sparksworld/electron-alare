{
  "name": "arale",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon -w src --exec 'npm run compile' -e ts",
    "compile": "tsc -d && electron ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "spark",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "electron": "^24.4.0",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.4"
  }
}
